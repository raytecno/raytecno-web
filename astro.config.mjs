import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static', // Genera HTML estático
  site: 'https://tu-static-web-app.azurestaticapps.net', // Cambia por tu URL
});
