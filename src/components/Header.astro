---
/**
 * Header.astro - Navegación principal con selector de idiomas
 * Con logo SVG inline (no imagen externa)
 */
import { getPageUrl, type Lang } from "@/config/pages";
import { getTranslations } from "@/i18n";

interface Props {
  lang?: Lang;
}

const { lang = "es" } = Astro.props;
const t = getTranslations(lang);

// Detectar página actual
const currentPath = Astro.url.pathname;

// Función para verificar si un link está activo
const isActive = (path: string) => {
  // Normalizar paths (quitar trailing slash)
  const normalizedCurrent = currentPath.replace(/\/$/, "");
  const normalizedPath = path.replace(/\/$/, "");

  // Para home, debe ser exacto
  if (normalizedPath === `/${lang}` || normalizedPath === `/${lang}/`) {
    return (
      normalizedCurrent === `/${lang}` ||
      normalizedCurrent === `/${lang}/` ||
      currentPath === `/${lang}/`
    );
  }

  // Para otras páginas, verificar si empieza con el path
  return normalizedCurrent.startsWith(normalizedPath);
};

// Idiomas soportados
const languages: { code: Lang; name: string; flag: string }[] = [
  { code: "es", name: "Español", flag: "🇪🇸" },
  { code: "en", name: "English", flag: "🇬🇧" },
  { code: "fr", name: "Français", flag: "🇫🇷" },
  { code: "ca", name: "Català", flag: "🏴󠁥󠁳󠁣󠁴󠁿" },
  { code: "pt-br", name: "Português", flag: "🇧🇷" },
];

const currentLang = languages.find((l) => l.code === lang) || languages[0];

// URLs de navegación
const homeUrl = `/${lang}/`;
const modulosUrl = getPageUrl("modulos", lang);
const preciosUrl = getPageUrl("precios", lang);
const tecnologiaUrl = getPageUrl("tecnologia", lang);
const blogUrl = `/${lang}/blog`; // ← NUEVO: URL del Blog
const contactoUrl = getPageUrl("contacto", lang);

// Traducciones del Blog por idioma
const blogLabel: Record<Lang, string> = {
  es: "Blog",
  en: "Blog",
  fr: "Blog",
  ca: "Blog",
  "pt-br": "Blog",
};
---

<header class="header">
  <div class="header-container">
    <!-- Logo con SVG inline -->
    <a href={`/${lang}/`} class="logo">
      <svg class="logo-icon" viewBox="0 0 40 40" fill="none">
        <path
          d="M20 2L37 12V28L20 38L3 28V12L20 2Z"
          fill="url(#grad1)"
          stroke="#fff"
          stroke-width="1"></path>
        <path
          d="M20 8L30 14V26L20 32L10 26V14L20 8Z"
          fill="rgba(255,255,255,0.3)"></path>
        <defs>
          <linearGradient id="grad1" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" style="stop-color:#667eea"></stop>
            <stop offset="100%" style="stop-color:#764ba2"></stop>
          </linearGradient>
        </defs>
      </svg>
      <div class="logo-text">
        <span class="logo-name">RAYGOLD</span>
        <span class="logo-tagline">Software para Joyería</span>
      </div>
    </a>

    <!-- Navegación principal -->
    <nav class="nav-main">
      <a href={homeUrl} class:list={["nav-link", { active: isActive(homeUrl) }]}
        >{t.nav?.home || "Inicio"}</a
      >
      <a
        href={modulosUrl}
        class:list={["nav-link", { active: isActive(modulosUrl) }]}
        >{t.nav?.modules || "Módulos"}</a
      >
      <a
        href={preciosUrl}
        class:list={["nav-link", { active: isActive(preciosUrl) }]}
        >{t.nav?.pricing || "Precios"}</a
      >
      <a
        href={tecnologiaUrl}
        class:list={["nav-link", { active: isActive(tecnologiaUrl) }]}
        >{t.nav?.technology || "Tecnología"}</a
      >
      <!-- NUEVO: Enlace al Blog -->
      <a href={blogUrl} class:list={["nav-link", { active: isActive(blogUrl) }]}
        >{blogLabel[lang]}</a
      >
      <a
        href={contactoUrl}
        class:list={["nav-link", { active: isActive(contactoUrl) }]}
        >{t.nav?.contact || "Contacto"}</a
      >
    </nav>

    <!-- Selector de idioma -->
    <div class="lang-selector">
      <button
        class="lang-trigger"
        aria-label="Seleccionar idioma"
        aria-expanded="false"
      >
        <span class="lang-flag">{currentLang.flag}</span>
        <span class="lang-name">{currentLang.name}</span>
        <svg
          class="lang-chevron"
          width="12"
          height="12"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <path d="M6 9l6 6 6-6"></path>
        </svg>
      </button>
      <div class="lang-dropdown">
        {
          languages.map((l) => (
            <a
              href={`/${l.code}/`}
              class:list={["lang-option", { active: l.code === lang }]}
              hreflang={l.code}
            >
              <span class="lang-option-flag">{l.flag}</span>
              <span class="lang-option-name">{l.name}</span>
              {l.code === lang && (
                <svg
                  class="lang-check"
                  width="16"
                  height="16"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <path d="M20 6L9 17l-5-5" />
                </svg>
              )}
            </a>
          ))
        }
      </div>
    </div>

    <!-- Menú móvil -->
    <button class="mobile-toggle" aria-label="Menú">
      <svg
        width="24"
        height="24"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
      >
        <path d="M3 12h18M3 6h18M3 18h18"></path>
      </svg>
    </button>
  </div>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(15, 23, 42, 0.95);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .header-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 24px;
    height: 72px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  /* Logo */
  .logo {
    display: flex;
    align-items: center;
    gap: 12px;
    text-decoration: none;
  }

  .logo-icon {
    width: 40px;
    height: 40px;
  }

  .logo-text {
    display: flex;
    flex-direction: column;
  }

  .logo-name {
    font-size: 1.1rem;
    font-weight: 700;
    color: white;
    letter-spacing: 2px;
  }

  .logo-tagline {
    font-size: 0.65rem;
    color: #94a3b8;
    letter-spacing: 0.5px;
  }

  /* Navegación */
  .nav-main {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-left: auto;
  }

  .nav-link {
    padding: 8px 16px;
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    border-radius: 8px;
    transition: all 0.2s ease;
  }

  .nav-link:hover {
    color: white;
    background: rgba(255, 255, 255, 0.1);
  }

  .nav-link.active {
    color: white;
    background: rgba(102, 126, 234, 0.25);
    border: 1px solid rgba(102, 126, 234, 0.4);
  }

  /* Selector de idioma */
  .lang-selector {
    position: relative;
    margin-left: 32px;
  }

  .lang-trigger {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    color: white;
    font-size: 0.85rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
    min-width: 130px;
  }

  .lang-trigger:hover {
    background: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.3);
  }

  .lang-flag {
    font-size: 1.1rem;
  }

  .lang-name {
    font-weight: 500;
    flex: 1;
  }

  .lang-chevron {
    transition: transform 0.2s ease;
  }

  .lang-dropdown {
    position: absolute;
    top: calc(100% + 8px);
    right: 0;
    min-width: 180px;
    background: #1e293b;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 8px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.2s ease;
    z-index: 100;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
  }

  .lang-selector:hover .lang-dropdown {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .lang-option {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 12px;
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    border-radius: 8px;
    transition: all 0.15s ease;
  }

  .lang-option:hover {
    background: rgba(255, 255, 255, 0.1);
    color: white;
  }

  .lang-option.active {
    background: rgba(102, 126, 234, 0.2);
    color: #a5b4fc;
  }

  .lang-option-flag {
    font-size: 1.25rem;
  }

  .lang-option-name {
    flex: 1;
    font-size: 0.9rem;
  }

  .lang-check {
    color: #a5b4fc;
  }

  /* Menú móvil */
  .mobile-toggle {
    display: none;
    padding: 8px;
    background: transparent;
    border: none;
    color: white;
    cursor: pointer;
  }

  @media (max-width: 900px) {
    .nav-main {
      display: none;
    }

    .mobile-toggle {
      display: block;
    }

    .lang-name {
      display: none;
    }

    .lang-trigger {
      min-width: auto;
      padding: 8px 12px;
    }

    .logo-tagline {
      display: none;
    }
  }
</style>

<script>
  document.querySelectorAll(".lang-trigger").forEach((trigger) => {
    trigger.addEventListener("click", () => {
      const expanded = trigger.getAttribute("aria-expanded") === "true";
      trigger.setAttribute("aria-expanded", (!expanded).toString());
    });
  });

  document.addEventListener("click", (e) => {
    const target = e.target as HTMLElement;
    if (!target.closest(".lang-selector")) {
      document.querySelectorAll(".lang-trigger").forEach((trigger) => {
        trigger.setAttribute("aria-expanded", "false");
      });
    }
  });
</script>
