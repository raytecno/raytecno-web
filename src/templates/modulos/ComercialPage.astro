---
/**
 * Template: Módulo Comercial
 * Ventas, pedidos y facturación
 */
import SubmoduloLayout from "@/components/SubmoduloLayout.astro";

interface Props {
  lang: string;
  pageId: string;
}

const { lang } = Astro.props;
---

<SubmoduloLayout lang={lang} moduloId="comercial" />
