---
/**
 * Template: Módulo Portal
 * Portal de clientes: consulta de pedidos y facturas
 */
import SubmoduloLayout from "@/components/SubmoduloLayout.astro";

interface Props {
  lang: string;
  pageId: string;
}

const { lang } = Astro.props;
---

<SubmoduloLayout lang={lang} moduloId="portal" />
